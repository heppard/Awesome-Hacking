

on:
  schedule:
    - cron: '0 * * * *'
  

permissions:
  issues: write
  pull-requests: write
  discussions: write

concurrency:


jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - uses: dessant/lock-threads@v5
        with:
          issue-inactive-days: '7'
          pr-inactive-days: '7'
